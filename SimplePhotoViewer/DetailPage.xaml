<Page
    x:Class="SimplePhotoViewer.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimplePhotoViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    
    NavigationCacheMode="Enabled">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Height="565" Width="692">

        <Button Click="GoBack_ClickHandler" Opacity="0.995" Content="GoBack" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="92,0,0,0" Width="72"/>

        <FlipView x:Name="DetailPageFlipView" 
                  ItemsSource="{x:Bind Mode=OneWay, Path=ImageSkus}" 
                  Opacity="1" 
                  Visibility="Visible">
            <FlipView.ItemTemplate>
                <DataTemplate x:DataType="local:ImageSku">
                    <Image x:Name="ItemImage"
                                    Opacity="1" 
                                    Height="200"
                                    Width="200"
                                    Margin="4"
                                    Source="{x:Bind Mode=OneWay, Path=ImageThumbnail}"
                                    Stretch="Uniform"/>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
        <!--<ScrollViewer>
            <GridView x:Name="ImageGridView"
                  Margin="0,0,0,8"
                  IsItemClickEnabled="True"
                  ItemsSource="{x:Bind ImageSkus, Mode=OneWay}"
                  SelectionMode="Extended" Opacity="1"
                  >
                <GridView.ItemTemplate>
                    <DataTemplate 
                      x:DataType="local:ImageSku">

                        <StackPanel>
                            <Image x:Name="ItemImage"
                                    Opacity="1" 
                                    Height="200"
                                    Width="200"
                                    Margin="4"
                                    Source="{x:Bind ImageThumbnail, Mode=OneWay}"
                                    Stretch="Uniform">
                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind ImageName, Mode=OneWay}"/>
                                </ToolTipService.ToolTip>
                            </Image>
                            <TextBlock Text="{x:Bind ImageNameWithType, Mode=OneWay}" TextAlignment="Center"></TextBlock>
                        </StackPanel>

                    </DataTemplate>
                </GridView.ItemTemplate>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>
        </ScrollViewer>-->
    </StackPanel>
</Page>
